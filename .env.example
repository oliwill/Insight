# Insight 环境配置
# 1. 复制为 .env：cp .env.example .env
# 2. 将路径改成你的 Obsidian vault / Trader workspace 真实路径
# 3. API key 留空时，系统会使用免费数据源或跳过对应增强模块

# ── 长桥 API（可选，不填则自动 fallback 到 Yahoo Finance）──
LONGBRIDGE_APP_KEY=
LONGBRIDGE_APP_SECRET=
LONGBRIDGE_ACCESS_TOKEN=

# ── 搜索 API（可选；不填则使用 Yahoo Finance News + DuckDuckGo HTML）──
SERPAPI_KEY=
NEWSAPI_KEY=

# ── X / Twitter MCP（可选；不填则跳过 KOL 观点抓取）──
# 凭据来自 https://console.x.com 开发者后台。任一凭据存在即启用 KOLFetcher。
# 推荐 App-only Bearer Token（只读，免 OAuth 跳转）。
X_API_BEARER_TOKEN=
X_CLIENT_ID=
X_CLIENT_SECRET=
# 传输方式：local=本地 xmcp/xurl 子进程（默认）| remote=docs.x.com 托管端点
X_MCP_MODE=local
# 定向关注 KOL 句柄，逗号分隔，不带 @（例：Serenity,Compound24,UnusualWhales）
KOL_WATCHLIST=

# ── Obsidian Trader workspace ──
# 建议指向 Trader 工作区根目录；Analysis、Materials、Tasks、Watchlist、Charts 会位于此目录下。
# 示例（macOS/Linux）：/home/you/ObsidianVault/Trader
# 示例（Windows）：    C:/Users/you/ObsidianVault/Trader
WIKI_BASE_DIR=/path/to/your/obsidian/vault/Trader
WIKI_SUBDIR=Analysis
MATERIALS_SUBDIR=Materials

# ── Obsidian 全局目录（可放在 vault 根目录或 Trader workspace 内）──
OBSIDIAN_INBOX_DIR=/path/to/your/obsidian/vault/Inbox
OBSIDIAN_TASKS_DIR=/path/to/your/obsidian/vault/Trader/Tasks
OBSIDIAN_DASHBOARD_PATH=/path/to/your/obsidian/vault/Dashboard.md

# ── Inbox watcher / Podwise 输入目录（可选）──
OBSIDIAN_CLIPPINGS_DIR=/path/to/your/obsidian/vault/Clippings
OBSIDIAN_RAW_DIR=/path/to/your/obsidian/vault/Raw

# ── Telegram bot / Telegram 通知（可选）──
TELEGRAM_BOT_TOKEN=
TELEGRAM_USER_ID=

# ── Podwise 同步（可选）──
PODWISE_CLI_PATH=podwise
PODWISE_SYNC_DAYS=7
PODWISE_OUTPUT_DIR=

# ── 分析超时保护 ──
ANALYSIS_TIMEOUT=30

# ── Python scheduler.py 守护进程配置（macOS/Linux）──
SCHEDULE_SCAN_INBOX=*/30 * * * *
SCHEDULE_REVIEW=0 9 * * *
SCHEDULE_DASHBOARD=0 8 * * *
SCHEDULE_NOTIFY=true
